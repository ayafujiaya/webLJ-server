<!DOCTYPE html>

<html>
  <head>
  <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.0/jquery.min.js"></script>
<link type="text/css" rel="stylesheet" href="http://code.jquery.com/ui/1.10.3/themes/cupertino/jquery-ui.min.css" />
<script type="text/javascript" src="http://code.jquery.com/ui/1.10.3/jquery-ui.min.js"></script>
<script type="text/javascript" src="/javascripts/jquery.ui.touch-punch.js"></script>
<script type="text/javascript">
var color = 0;
var strobo = 0;
var rotate = 0;

$(function() {
  // 2スライダーを適用
  $('#slider').slider({
    min: 0,
    max: 127,
    step: 2,
    value: 0,
    // 3スライダーの変化時にテキストボックスの値表示を更新
    change: function(e, ui) {
      $('#num').val(ui.value);
color = ui.value;
    },
    // 4スライダーの初期化時に、その値をテキストボックスにも反映
    create: function(e, ui) {
      $('#num').val($(this).slider('option', 'value'));
    }
  });

  $('#slider_2').slider({
    min: 0,
    max: 127,
    step: 2,
    value: 0,
    // 3スライダーの変化時にテキストボックスの値表示を更新
    change: function(e, ui) {
      $('#num_2').val(ui.value);
rotate = ui.value;
    },
    // 4スライダーの初期化時に、その値をテキストボックスにも反映
    create: function(e, ui) {
      $('#num_2').val($(this).slider('option', 'value'));
    }
  });

  $('#slider_3').slider({
    min: 0,
    max: 127,
    step: 2,
    value: 0,
    // 3スライダーの変化時にテキストボックスの値表示を更新
    change: function(e, ui) {
      $('#num_3').val(ui.value);
strobo = ui.value;
    },
    // 4スライダーの初期化時に、その値をテキストボックスにも反映
    create: function(e, ui) {
      $('#num_3').val($(this).slider('option', 'value'));
    }
  });

});
</script>
  <link rel="stylesheet" href="/stylesheets/site.css" />
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1,  user-scalable=no" >
<script src="/socket.io/socket.io.js"></script>
<script>

</script>
<script type="text/javascript" src="/javascripts/socket_connect.js"></script>
  </head>

  <body>
  <div id="container" style="">
  <header style="">
  <div id="connectId"></div>
  <div id="type"></div>
  <div id="receiveMsg"></div>
  </header>
  <div id="mainBox" style="">

<hr>
<input style="" id="num" type="text" size="3" readonly />
色です。</br>
<div id="slider" style="width:260px; margin:0 30px 0 30px;"></div>
<hr>
<input id="num_2" type="text" size="3" readonly />照明の点滅の速度です。0 or 127推奨</br>
<div id="slider_2" style="width:260px; margin:0 30px 0 30px;"></div>
<hr>
<input id="num_3" type="text" size="3" readonly />照明の回転です。</br>
<div id="slider_3" style="width:260px; margin:0 30px 0 30px;"></div>
<hr>
  <div id="sensor" style="display:none;"></div>
  <script type="text/javascript" src="/javascripts/shake_track.js"></script>

  </div>
  <footer>
  </footer>
  </div>
  </div> 

  
  </body>
</html>
